program ejemplo_error

 int a, b, c ;
 float ;
 int d;
 e;
 float f, h;
 
 proc  ( int <= x; int y; int <=> resul)
 	int aux;
 	{
		int iteraciones;
		aux = x;
		int resul;
		do {  
			resul = resul + 1;
			skip if resul > 1000000;
			aux = aux - 1;
			iteraciones = iteraciones + 1; 
			} until  aux == 0 
		else {
			if resul < 0 {
				exit;
			};
		};
 	}
{
	read(a);
	read;
} 
