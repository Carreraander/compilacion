program ejemplo 
  int a,b,c; [*comentario*]
  float [*comentario*] d,e ;
  [*comentario*] proc sumar ( int <= x; int <= y; int <=> resul)  [*comentario
  multilinea*] 
    int aux, iteraciones;
    {
      aux = x;
      resul = y;  
      if resul < 1000 { 
        iteraciones = 0;  
        do {  
          resul = [*comentario*] resul + 1;
          skip if resul > 1000000;
          aux = aux - 1;
          iteraciones = iteraciones + 1; 
        } until  aux == 0 
        else {
          if resul < 0 {
              exit;
          };
          println(iteraciones);
        };  #fin del do  
      }; #fin del if
    } #fin del proc
  { 
    read(a); 
    read(b);
    d= 1/b; [*comentario 
    multilinea*]
    c= c*(c*d)+e;
    println(c);
  }