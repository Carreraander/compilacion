program ejemplo_error

 int a, b, c ;
 [*No debe contener una variable a la hora de declararse (hay que especificar un tamaño)*]
 float a[e];
 int d;
 float f, h;
 
 proc  ( int <= x; int y; int <=> resul)
 	int aux;
 	{
		int iteraciones;
		aux = x;
		int resul;
		do {  
			resul = resul + 1;
			skip if resul > 1000000;
			[*Array de una dimension intentando utilizar dos*]
			aux = a[e][b] - 1;
			iteraciones = iteraciones + 1; 
			} until  aux == 0 
		else {
			if resul < 0 {
				exit;
			};
		};
 	}
{
	read(a);
	read;
} 
